#include <stdio.h>

int main() {
    printf("Add 1 + 1 and get %d\n", 1 + 1);
    printf("What about 2 - 1? -> %d\n", 2 - 1);
    printf("13 * 12 is so hard! or is it? ->%d\n", 13 * 12);
    printf("45 / 4 gives %d\n", 45 / 4); // only the quotient
    printf("45 %% 4 gives %d the remainder!", 45 % 4);
    // See the weird notations? We are gonna explain it shortly!

    return 0;
}
